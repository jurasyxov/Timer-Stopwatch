using System;
using System.Threading;

class Program
{
    static void Main()
    {
        int seconds = 0;

        while (true)
        {
            Thread.Sleep(1000);
            seconds++;
            Console.WriteLine($"Elapsed: {seconds}s");
        }
    }
}
